std::vector<int> UniqueNumber(std::vector<int>& nums) {
    int all_bits = 0;
    for (int n : nums)
        all_bits ^= n;

    int diff_bit = all_bits & -all_bits;

    int a = 0, b = 0;
    for (int n : nums) {
        if (n & diff_bit) a ^= n;
        else b ^= n;
    }

    return {a, b};
}
